defmodule Mix.Tasks.ImportDataFromCsvTest do
  # TODO write tests for the following:

  # creates new food trucks records cooresponding to columns in CSV

  # if record already exists for a location_id, does not add food truck from CSV

  # creates new food item if food item in CSV does not exist in db

  # if food item with that name already exists, does not add another record to db

  # creates new food_truck_food_item record for a new food truck/food item combination

  # does not create new food_truck_food_item record if record already exists for that combo
end
